

generator client {
  provider      = "prisma-client-js"
  binaryTargets = ["native", "debian-openssl-1.1.x"]
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}


model Member {
  id          String   @id @default(uuid())
  surname    String
  firstname    String
  phoneNumber String   @unique
  email String   @unique
  voicePart   String?
  isActive    Boolean  @default(true)
  createdAt   DateTime @default(now())
  dateOfBirth DateTime?
  gender        String
profile_pic  String?
  tasks       Task[]
  
}

model Task {
  id              String   @id @default(uuid())
  memberId        String
  notes        String
  performanceDate DateTime
  isTaskDone       Boolean @default(false)
  reminderSent    Boolean  @default(false)
  // When a reminder was last actually sent for this task. Used as a cooldown
  // (see REMINDER_COOLDOWN_HOURS in Notification.controller.ts) rather than
  // a one-shot flag, so the legitimate Monday/Wednesday/Saturday reminders
  // (days apart) all still fire, while accidental rapid re-firing within a
  // short window (a misconfigured test schedule, a duplicate trigger) does not.
  lastReminderSentAt DateTime?
  createdAt       DateTime @default(now())
  member          Member   @relation(fields: [memberId], references: [id], onDelete: Cascade)
  role             roleType
  @@index([performanceDate])
}


enum roleType {
  RESPNSORIAL_PASALM
  COMMUNION_SOLO
  
}
 // @@unique([memberId, performanceDate])